server:
  port: 8084
  tomcat:
    uri-encoding: UTF-8
#  domain: https://www.ecnuers.cn

spring:
#  http:
#    encoding:
#      charset: UTF-8
#      force: true
#      enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://1.15.30.214:3306/yiqikang?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: abc123400197

  jackson:
    time-zone: GMT+8

  redis:
    database: 0
    host: xx.xx.xx.xx
    port: 6379
    password: xxx
    timeout: 1000

mybatis:
  configuration:
    map-underscore-to-camel-case: true

mini-app:
  app-id: wxd44aeb63d64b8ce4
  app-secret: 431c14ca14b1b6200e818fc3b6f6ab9d

#日志路径
logback:
  dir: logs
  #文件名称
  filename:
    all: springboot-base-frame-all
    info: springboot-base-frame-info
    warn: springboot-base-frame-warn
    error: springboot-base-frame-error
  #文件的最大数量
  maxhistory: 10
  #文件的最大文件大小
  maxfilesize: 100MB
  #日志级别
  level: debug
  #日志字符编码
  charset: UTF-8
  #日志格式
  CONSOLE_LOG_PATTERN: '%d{yyyy-MM-dd HH:mm:ss.SSS} %boldYellow([%thread]) %highlight(%-5level) %boldGreen(%logger{50}) - %msg%n'
  FILE_LOG_PATTERN: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n'